# Private Sanctuary OGP Worker

Cloudflare Worker for URL preview cards.

## Deploy

```bash
npm install -g wrangler
wrangler login
wrangler deploy
```

After deployment, copy the Worker URL and paste it into the app's detailed settings as `OGP WORKER URL`.
